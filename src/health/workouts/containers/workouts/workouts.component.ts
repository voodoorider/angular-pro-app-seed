import { Component, OnInit } from '@angular/core';

@Component({
  moduleId: module.id,
  selector: 'workouts',
  template: `
    <div>Workouts</div>`
})

export class WorkoutsComponent implements OnInit {
  constructor() {
  }

  ngOnInit() {
  }
}
